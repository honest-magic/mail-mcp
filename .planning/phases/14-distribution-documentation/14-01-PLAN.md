---
phase: 14-distribution-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .github/workflows/publish.yml
autonomous: true
requirements:
  - DOC-01
  - DIST-01

must_haves:
  truths:
    - "README contains an Updating section with npx, npm, and Homebrew subsections"
    - "README contains an install-method comparison table with Method, Command, Best for, Auto-updates columns"
    - "README contains a Homebrew subsection under Installation"
    - "publish.yml creates a GitHub Release when a version tag is pushed"
  artifacts:
    - path: "README.md"
      provides: "Updating section, Homebrew install subsection, comparison table"
      contains: "## Updating"
    - path: ".github/workflows/publish.yml"
      provides: "GitHub Release creation step"
      contains: "softprops/action-gh-release@v2"
  key_links:
    - from: "README.md"
      to: "https://github.com/honest-magic/mail-mcp/releases"
      via: "Markdown link in Updating section"
      pattern: "github.com/honest-magic/mail-mcp/releases"
    - from: ".github/workflows/publish.yml"
      to: "GitHub Releases API"
      via: "softprops/action-gh-release@v2 step"
      pattern: "softprops/action-gh-release@v2"
---

<objective>
Update README with Homebrew install instructions, an Updating guide, and install-method comparison table. Add GitHub Release creation to the publish workflow.

Purpose: Users can discover all install methods, know how to update, and see release notes on GitHub.
Output: Updated README.md and publish.yml in this repository.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@.github/workflows/publish.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Homebrew install subsection, Updating section, and comparison table to README</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content and section ordering)
    - package.json (package name: @honest-magic/mail-mcp, version: 1.1.0)
    - .planning/phases/14-distribution-documentation/14-CONTEXT.md (decisions D-06 through D-11)
  </read_first>
  <action>
Edit README.md to add three blocks of content. Preserve all existing content unchanged.

**1. Add `### Homebrew` subsection under `## Installation` (per D-11)**

Insert after the existing `### Global install` subsection (after line 23). Content:

```markdown
### Homebrew

```bash
brew tap honest-magic/tap
brew install mail-mcp
```
```

**2. Add `## Updating` section directly after `## Installation` (per D-06)**

Insert a new `## Updating` section between the end of `## Installation` (after the new Homebrew subsection) and before `## Configuration`. Content (per D-07, D-08, D-09):

```markdown
## Updating

### npx (run without installing)

npx always fetches the latest published version. No update step needed.

### Global npm install

```bash
npm update -g @honest-magic/mail-mcp
```

### Homebrew

```bash
brew upgrade mail-mcp
```

### Version pinning

To run a specific version instead of latest:

```bash
npx @honest-magic/mail-mcp@1.1.0
```

See the [Releases page](https://github.com/honest-magic/mail-mcp/releases) for version history.
```

**3. Add install-method comparison table (per D-10)**

Insert the comparison table at the top of `## Installation`, between the `## Installation` heading and the `### Run without installing` subsection:

```markdown
| Method | Install command | Best for | Auto-updates? |
|--------|----------------|----------|---------------|
| npx (no install) | `npx @honest-magic/mail-mcp` | Trying it out, always-latest | Yes -- on each run |
| Global npm | `npm install -g @honest-magic/mail-mcp` | Daily use, offline-friendly | No -- run `npm update -g` |
| Homebrew | `brew tap honest-magic/tap && brew install mail-mcp` | macOS users who prefer brew | No -- run `brew upgrade` |
```
  </action>
  <verify>
    <automated>grep -n "## Updating" README.md && grep -n "### Homebrew" README.md && grep -n "brew tap honest-magic/tap" README.md && grep -n "Version pinning" README.md && grep -n "npx @honest-magic/mail-mcp@1.1.0" README.md && grep -n "github.com/honest-magic/mail-mcp/releases" README.md && grep -c "| Method" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `## Updating` heading
    - README.md contains `### Homebrew` under Installation with `brew tap honest-magic/tap`
    - README.md contains `### npx (run without installing)` under Updating
    - README.md contains `### Global npm install` under Updating
    - README.md contains `### Homebrew` under Updating with `brew upgrade mail-mcp`
    - README.md contains `### Version pinning` with `npx @honest-magic/mail-mcp@1.1.0`
    - README.md contains a link to `https://github.com/honest-magic/mail-mcp/releases`
    - README.md contains a table row with columns: Method, Install command, Best for, Auto-updates?
    - The `## Updating` section appears after `## Installation` and before `## Configuration`
    - The comparison table appears between `## Installation` heading and `### Run without installing`
    - All existing README content is preserved (no deletions)
  </acceptance_criteria>
  <done>README has Homebrew install instructions, a complete Updating section with 3 method subsections + version pinning + Releases link, and an install-method comparison table.</done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Release step to publish.yml</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml (current workflow structure, permissions block at line 33-35)
    - .planning/phases/14-distribution-documentation/14-RESEARCH.md (Pattern 3: GitHub Release step, permission requirements)
    - .planning/phases/14-distribution-documentation/14-CONTEXT.md (decisions D-12, D-13)
  </read_first>
  <action>
Edit `.github/workflows/publish.yml` to add a GitHub Release creation step. Two changes required:

**1. Update the `publish` job permissions block (line 33-35)**

Change from:
```yaml
    permissions:
      contents: read
      id-token: write
```

To:
```yaml
    permissions:
      contents: write
      id-token: write
```

Only the `publish` job permissions change. The `ci` job has no explicit permissions block and must NOT get one.

**2. Add a new step at the end of the `publish` job (after line 67 "Publish to npm")**

Append this step:
```yaml
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
```

This step runs after npm publish succeeds. It uses the tag that triggered the workflow to create a release with auto-generated notes (per D-12, D-13). No manual release body needed.
  </action>
  <verify>
    <automated>grep "contents: write" .github/workflows/publish.yml && grep "softprops/action-gh-release@v2" .github/workflows/publish.yml && grep "generate_release_notes: true" .github/workflows/publish.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/publish.yml contains `contents: write` in the publish job permissions block
    - .github/workflows/publish.yml contains `id-token: write` (unchanged)
    - .github/workflows/publish.yml contains a step with `uses: softprops/action-gh-release@v2`
    - .github/workflows/publish.yml contains `generate_release_notes: true`
    - The GitHub Release step appears after the "Publish to npm" step
    - The `ci` job has NO permissions block (unchanged)
    - `npm test` passes (no source regressions)
  </acceptance_criteria>
  <done>publish.yml creates a GitHub Release with auto-generated notes when a version tag is pushed, after npm publish succeeds.</done>
</task>

</tasks>

<verification>
- `grep "## Updating" README.md` returns a match
- `grep "brew tap honest-magic/tap" README.md` returns a match
- `grep "softprops/action-gh-release@v2" .github/workflows/publish.yml` returns a match
- `grep "contents: write" .github/workflows/publish.yml` returns a match
- `npm test` passes
</verification>

<success_criteria>
- README contains Homebrew install instructions, Updating section with 3 methods + version pinning, comparison table, and Releases link
- publish.yml creates GitHub Releases on tag push with auto-generated notes
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-distribution-documentation/14-01-SUMMARY.md`
</output>
