---
phase: 08-github-repository
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - GH-02

must_haves:
  truths:
    - "A visitor can follow the README alone to install the server via npx or global install"
    - "The README shows a complete ACCOUNTS_JSON JSON structure"
    - "The README shows the Keychain command to store a password"
    - "The README shows how to configure OAuth2 via the register_oauth2_account tool"
    - "The README lists all 14 tools in a table with one-line descriptions"
    - "The README explains the --read-only flag"
    - "The README shows both a Claude Desktop config snippet and a generic MCP client snippet"
  artifacts:
    - path: "README.md"
      provides: "Consumer-facing documentation"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: "package.json#files"
      via: "README.md is in package.json files field and must exist before npm publish"
      pattern: "README.md"
---

<objective>
Create README.md at the repo root with consumer-facing documentation covering installation, configuration, the full tool list, and read-only mode.

Purpose: README.md is required by package.json's `files` field before npm publish works, and by the GitHub repo goal (GH-02) so any visitor can configure the server without consulting other documents.
Output: README.md at repo root — complete, concise, technical.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-github-repository/08-CONTEXT.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>

  <read_first>
    - /Users/mis/dev/mail_mcp/package.json — for exact package name, bin key, engines, version
    - /Users/mis/dev/mail_mcp/.planning/phases/08-github-repository/08-CONTEXT.md — locked decisions on sections, tone, credential walkthrough
    - /Users/mis/dev/mail_mcp/src/index.ts — confirm exact tool names and current descriptions
  </read_first>

  <action>
Create /Users/mis/dev/mail_mcp/README.md with the content below. Tone: concise, technical — no marketing copy. No emojis.

Adjust tool names only if src/index.ts reveals a name that differs from the list. Do not add badges, a Contributing section, or emojis.

README content (write verbatim):

    # mail-mcp

    MCP server for IMAP/SMTP email access — works with Claude and other MCP clients.

    ## Requirements

    - Node.js >=18
    - macOS (credentials stored in Keychain)

    ## Installation

    ### Run without installing (recommended)

    ```bash
    npx @honest-magic/mail-mcp
    ```

    ### Global install

    ```bash
    npm install -g @honest-magic/mail-mcp
    mail-mcp
    ```

    ## Configuration

    ### 1. Define accounts

    Set the `ACCOUNTS_JSON` environment variable to a JSON array of account objects:

    ```json
    [
      {
        "id": "work",
        "name": "Work Email",
        "host": "imap.example.com",
        "port": 993,
        "smtpHost": "smtp.example.com",
        "smtpPort": 587,
        "user": "you@example.com",
        "authType": "login",
        "useTLS": true
      }
    ]
    ```

    Fields:
    | Field | Type | Required | Description |
    |-------|------|----------|-------------|
    | `id` | string | yes | Unique identifier used by MCP tools |
    | `name` | string | yes | Human-readable label |
    | `host` | string | yes | IMAP hostname |
    | `port` | number | yes | IMAP port (993 for TLS, 143 for STARTTLS) |
    | `smtpHost` | string | no | SMTP hostname (omit for read-only use) |
    | `smtpPort` | number | no | SMTP port (587 for STARTTLS, 465 for TLS) |
    | `user` | string | yes | Login username / email address |
    | `authType` | string | yes | `login` or `oauth2` |
    | `useTLS` | boolean | yes | `true` for implicit TLS; `false` for STARTTLS |

    ### 2. Store credentials

    **Password / app password** — add to macOS Keychain once:

    ```bash
    security add-generic-password \
      -s com.mcp.mail-server \
      -a <account-id> \
      -w <password-or-app-password>
    ```

    Replace `<account-id>` with the `id` from your `ACCOUNTS_JSON` (e.g. `work`).

    **OAuth2** — after starting the server, call the `register_oauth2_account` MCP tool:

    ```json
    {
      "tool": "register_oauth2_account",
      "arguments": {
        "accountId": "work",
        "accessToken": "<oauth2-access-token>",
        "refreshToken": "<oauth2-refresh-token>",
        "expiresAt": 1700000000000
      }
    }
    ```

    The token is stored in Keychain under the same service name. Refresh is handled automatically.

    ### 3. Add to your MCP client

    **Claude Desktop** (`~/Library/Application Support/Claude/claude_desktop_config.json`):

    ```json
    {
      "mcpServers": {
        "mail": {
          "command": "npx",
          "args": ["-y", "@honest-magic/mail-mcp"],
          "env": {
            "ACCOUNTS_JSON": "[{\"id\":\"work\",\"name\":\"Work\",\"host\":\"imap.example.com\",\"port\":993,\"user\":\"you@example.com\",\"authType\":\"login\",\"useTLS\":true}]"
          }
        }
      }
    }
    ```

    **Generic MCP client**:

    ```json
    {
      "mcpServers": {
        "mail": {
          "command": "mail-mcp",
          "env": {
            "ACCOUNTS_JSON": "..."
          }
        }
      }
    }
    ```

    Use `"command": "mail-mcp"` if installed globally, or `"command": "npx", "args": ["-y", "@honest-magic/mail-mcp"]` otherwise.

    ## Available Tools

    | Tool | Description |
    |------|-------------|
    | `list_accounts` | List all configured email accounts |
    | `list_emails` | List recent emails from a folder with metadata and snippets |
    | `search_emails` | Search emails by sender, subject, date range, or keywords |
    | `read_email` | Fetch the full content of an email as Markdown |
    | `send_email` | Send a new email via SMTP |
    | `create_draft` | Save a draft to the Drafts folder without sending |
    | `list_folders` | List all available folders and labels in a mailbox |
    | `move_email` | Move a message to another folder (Archive, Trash, Spam, etc.) |
    | `modify_labels` | Add or remove IMAP flags / provider labels on a message |
    | `get_thread` | Fetch all messages in a conversation thread |
    | `get_attachment` | Download attachment content via MCP Resource URI |
    | `extract_attachment_text` | Extract plain text from PDF and document attachments |
    | `register_oauth2_account` | Store OAuth2 tokens in Keychain for an account |
    | `batch_operations` | Apply move, delete, or label actions to multiple emails at once |

    ## Read-Only Mode

    Start the server with `--read-only` to disable all write operations:

    ```bash
    npx @honest-magic/mail-mcp --read-only
    ```

    In read-only mode:
    - Write tools (`send_email`, `create_draft`, `move_email`, `modify_labels`, `batch_operations`, `register_oauth2_account`) are removed from the tool list entirely and return a descriptive error if called directly.
    - SMTP authentication is skipped — only IMAP connects.
    - The active mode is advertised to the MCP client at handshake via `InitializeResult.instructions`.

    Claude Desktop read-only config:

    ```json
    {
      "mcpServers": {
        "mail-readonly": {
          "command": "npx",
          "args": ["-y", "@honest-magic/mail-mcp", "--read-only"],
          "env": {
            "ACCOUNTS_JSON": "..."
          }
        }
      }
    }
    ```

    ## License

    MIT
  </action>

  <verify>
    <automated>
      node -e "
        const fs = require('fs');
        const r = fs.readFileSync('/Users/mis/dev/mail_mcp/README.md','utf8');
        const checks = [
          ['list_accounts', r.includes('list_accounts')],
          ['list_emails', r.includes('list_emails')],
          ['search_emails', r.includes('search_emails')],
          ['read_email', r.includes('read_email')],
          ['send_email', r.includes('send_email')],
          ['create_draft', r.includes('create_draft')],
          ['list_folders', r.includes('list_folders')],
          ['move_email', r.includes('move_email')],
          ['modify_labels', r.includes('modify_labels')],
          ['get_thread', r.includes('get_thread')],
          ['get_attachment', r.includes('get_attachment')],
          ['extract_attachment_text', r.includes('extract_attachment_text')],
          ['register_oauth2_account', r.includes('register_oauth2_account')],
          ['batch_operations', r.includes('batch_operations')],
          ['ACCOUNTS_JSON', r.includes('ACCOUNTS_JSON')],
          ['security add-generic-password', r.includes('security add-generic-password')],
          ['com.mcp.mail-server', r.includes('com.mcp.mail-server')],
          ['--read-only', r.includes('--read-only')],
          ['mcpServers', r.includes('mcpServers')],
          ['npx @honest-magic/mail-mcp', r.includes('npx @honest-magic/mail-mcp')],
        ];
        const failed = checks.filter(([k,v]) => !v);
        if (failed.length) { console.error('MISSING:', failed.map(([k])=>k).join(', ')); process.exit(1); }
        console.log('README checks passed (' + checks.length + ')');
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - README.md exists at repo root
    - All 14 tool names present (verified by automated check above)
    - ACCOUNTS_JSON JSON block with id/name/host/port/user/authType/useTLS fields is present
    - `security add-generic-password -s com.mcp.mail-server` command is present verbatim
    - `register_oauth2_account` OAuth2 setup walkthrough is present
    - `--read-only` section explains tool filtering, SMTP skip, and instructions broadcast
    - Both Claude Desktop and generic MCP client config snippets are present
    - File has at least 100 lines (`wc -l README.md` >= 100)
  </acceptance_criteria>

  <done>README.md exists, passes all 20 automated checks, is committed to git.</done>
</task>

</tasks>

<verification>
Run automated node check from the verify block — all 20 assertions must pass. Also confirm `wc -l README.md` returns at least 100.
</verification>

<success_criteria>
- README.md at repo root
- All 14 tools documented in a table
- Full credential setup walkthrough: ACCOUNTS_JSON, Keychain command, OAuth2 via tool
- Both Claude Desktop and generic MCP config snippets shown
- --read-only section present and accurate
- File committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/08-github-repository/08-01-SUMMARY.md`
</output>
