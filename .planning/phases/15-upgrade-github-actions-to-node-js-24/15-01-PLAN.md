---
phase: 15-upgrade-github-actions-to-node-js-24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/publish.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - "Both workflows use node-version '22' consistently"
    - "All actions/checkout references use a Node.js 24-compatible version"
    - "All actions/setup-node references use a Node.js 24-compatible version"
    - "softprops/action-gh-release and actions/github-script are on their current stable versions"
    - "No FORCE_JAVASCRIPT_ACTIONS_TO_NODE24 env var is set in either workflow"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with node-version '22' and updated action versions"
      contains: "node-version: '22'"
    - path: ".github/workflows/publish.yml"
      provides: "Publish workflow confirming node-version '22' and updated action versions"
      contains: "node-version: '22'"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "actions/checkout"
      via: "uses: field"
      pattern: "actions/checkout@v4"
    - from: ".github/workflows/ci.yml"
      to: "actions/setup-node"
      via: "uses: field"
      pattern: "actions/setup-node@v4"
---

<objective>
Upgrade both GitHub Actions workflows to use Node.js 22 consistently and ensure all referenced actions use versions compatible with the upcoming Node.js 24 GitHub-hosted runner runtime. GitHub forces this migration on 2026-06-02.

Purpose: Prevent CI/CD breakage when GitHub deprecates older action runtime environments.
Output: Updated ci.yml and publish.yml with standardized node-version '22' and current action versions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-upgrade-github-actions-to-node-js-24/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade ci.yml — standardize Node.js version and action versions</name>
  <files>.github/workflows/ci.yml</files>

  <read_first>
    - .github/workflows/ci.yml — full file, understand all action references and node-version values
    - .planning/phases/15-upgrade-github-actions-to-node-js-24/15-CONTEXT.md — decisions D-01 through D-05
  </read_first>

  <action>
    Read .github/workflows/ci.yml in full. Make the following precise substitutions:

    1. Both occurrences of `node-version: '20'` → `node-version: '22'` (per D-01)
       - Line ~19: in the `ci` job setup-node step
       - Line ~43: in the `integration` job setup-node step

    2. Verify `actions/checkout` and `actions/setup-node` versions. The file currently uses:
       - `actions/checkout@v4` — keep as-is (v4 is the current major, compatible)
       - `actions/setup-node@v4` — keep as-is (v4 is the current major, compatible)

       If GitHub has released a v5 for either action at time of execution, upgrade to it. Otherwise leave at v4.

    3. Do NOT add `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` anywhere (per D-05).

    4. Optionally add a brief inline comment on the node-version line noting the standardization:
       `node-version: '22'  # standardized; was '20' (D-01)`
       This is at Claude's discretion — only add if it improves clarity without cluttering the file.

    The file has exactly 2 jobs (`ci` and `integration`), each with a `setup-node` step — both must be updated.
  </action>

  <verify>
    <automated>grep -n "node-version" /Users/mis/dev/mail_mcp/.github/workflows/ci.yml</automated>
  </verify>

  <acceptance_criteria>
    - `grep "node-version: '20'" .github/workflows/ci.yml` returns no matches
    - `grep "node-version: '22'" .github/workflows/ci.yml` returns exactly 2 matches (one per job)
    - `grep "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" .github/workflows/ci.yml` returns no matches
    - `grep "actions/checkout" .github/workflows/ci.yml` returns exactly 2 lines, all using a supported version
    - `grep "actions/setup-node" .github/workflows/ci.yml` returns exactly 2 lines, all using a supported version
    - File is valid YAML (no syntax errors)
  </acceptance_criteria>

  <done>Both jobs in ci.yml use node-version '22' and all action versions are Node.js 24-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update publish.yml — confirm node-version '22' and check all action versions</name>
  <files>.github/workflows/publish.yml</files>

  <read_first>
    - .github/workflows/publish.yml — full file, understand all action references, node-version values, and third-party actions
    - .planning/phases/15-upgrade-github-actions-to-node-js-24/15-CONTEXT.md — decisions D-03, D-04, D-05
  </read_first>

  <action>
    Read .github/workflows/publish.yml in full. The file has three jobs: `ci`, `publish`, and `update-homebrew`.

    1. Both `node-version` fields are already `'22'` — confirm they are unchanged. No edit needed for these.

    2. Check all action references in the file:
       - `actions/checkout@v4` (3 occurrences: ci job, publish job, update-homebrew job) — keep at v4 unless v5 is released
       - `actions/setup-node@v4` (2 occurrences: ci job, publish job) — keep at v4 unless v5 is released
       - `softprops/action-gh-release@v2` (1 occurrence: publish job) — per D-04, verify this version is current; keep at v2 unless there is a newer compatible major
       - `actions/github-script@v7` (1 occurrence: update-homebrew job) — per D-04, verify v7 is current; keep at v7 unless there is a newer compatible major

    3. If any action has a newer major version available that is confirmed Node.js 24 compatible, upgrade it. If uncertain, keep the current version.

    4. Do NOT add `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` anywhere (per D-05).

    5. Do NOT change any other workflow logic — only action version strings and node-version if somehow wrong.

    If no changes are needed, the task is still complete — confirm by grepping and documenting in the SUMMARY.
  </action>

  <verify>
    <automated>grep -n "node-version\|uses: actions\|uses: softprops" /Users/mis/dev/mail_mcp/.github/workflows/publish.yml</automated>
  </verify>

  <acceptance_criteria>
    - `grep "node-version: '20'" .github/workflows/publish.yml` returns no matches
    - `grep "node-version: '22'" .github/workflows/publish.yml` returns exactly 2 matches
    - `grep "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" .github/workflows/publish.yml` returns no matches
    - `grep "actions/checkout" .github/workflows/publish.yml` returns exactly 3 lines, all on a supported version
    - `grep "actions/setup-node" .github/workflows/publish.yml` returns exactly 2 lines, all on a supported version
    - `grep "softprops/action-gh-release" .github/workflows/publish.yml` returns 1 line on a current compatible version
    - `grep "actions/github-script" .github/workflows/publish.yml` returns 1 line on a current compatible version
    - File is valid YAML (no syntax errors)
  </acceptance_criteria>

  <done>All three jobs in publish.yml use node-version '22', all GitHub-provided actions are on Node.js 24-compatible versions, and all third-party actions are on their current compatible majors.</done>
</task>

</tasks>

<verification>
After both tasks:

```bash
# Confirm no '20' node-version remains anywhere in workflows
grep -r "node-version: '20'" /Users/mis/dev/mail_mcp/.github/workflows/

# Confirm both files use '22'
grep -r "node-version: '22'" /Users/mis/dev/mail_mcp/.github/workflows/

# Confirm no forced env var
grep -r "FORCE_JAVASCRIPT_ACTIONS_TO_NODE24" /Users/mis/dev/mail_mcp/.github/workflows/

# Quick YAML syntax check
node -e "
  const fs = require('fs');
  const yaml = require('js-yaml') 2>/dev/null || process.exit(0);
  ['ci.yml','publish.yml'].forEach(f => {
    yaml.load(fs.readFileSync('.github/workflows/' + f, 'utf8'));
    console.log(f + ': valid');
  });
" 2>/dev/null || echo "YAML check skipped (js-yaml not installed locally)"
```
</verification>

<success_criteria>
- Both .github/workflows/*.yml files have zero occurrences of `node-version: '20'`
- Both files consistently use `node-version: '22'` (per D-01)
- All action versions are compatible with the upcoming Node.js 24 GitHub runner runtime
- No `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` env var added (per D-05)
- `package.json` engines field is unchanged at `>=18` (per D-02, not touched by this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/15-upgrade-github-actions-to-node-js-24/15-01-SUMMARY.md` using the summary template at `~/.claude/get-shit-done/templates/summary.md`.

Include:
- Which action versions were kept vs upgraded (and why)
- Final node-version values confirmed in each workflow
- Any actions that were already at their current version (no-op)
</output>
