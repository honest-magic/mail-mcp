---
phase: 11-input-validation-safety-limits
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/validation.ts
  - src/utils/validation.test.ts
  - src/utils/rate-limiter.ts
  - src/utils/rate-limiter.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [VAL-02, SAFE-03]

must_haves:
  truths:
    - "validateEmailAddresses throws ValidationError for syntactically invalid addresses"
    - "validateEmailAddresses accepts valid addresses including angle-bracket format"
    - "AccountRateLimiter.consume() resolves when under the limit"
    - "AccountRateLimiter.consume() throws QuotaError when limit exceeded"
    - "Rate limits are per-account — exhausting account A does not affect account B"
  artifacts:
    - path: "src/utils/validation.ts"
      provides: "Email address validation utility"
      exports: ["validateEmailAddresses"]
    - path: "src/utils/validation.test.ts"
      provides: "Unit tests for email validation"
    - path: "src/utils/rate-limiter.ts"
      provides: "Per-account rate limiter wrapper"
      exports: ["AccountRateLimiter", "DEFAULT_RATE_LIMIT_POINTS", "DEFAULT_RATE_LIMIT_DURATION"]
    - path: "src/utils/rate-limiter.test.ts"
      provides: "Unit tests for rate limiter"
  key_links:
    - from: "src/utils/validation.ts"
      to: "src/errors.ts"
      via: "import ValidationError"
      pattern: "import.*ValidationError.*from.*errors"
    - from: "src/utils/rate-limiter.ts"
      to: "src/errors.ts"
      via: "import QuotaError"
      pattern: "import.*QuotaError.*from.*errors"
    - from: "src/utils/rate-limiter.ts"
      to: "rate-limiter-flexible"
      via: "import RateLimiterMemory"
      pattern: "import.*RateLimiterMemory.*from.*rate-limiter-flexible"
---

<objective>
Create the email validation utility and per-account rate limiter utility with full test coverage.

Purpose: These two independent utility modules are the foundation for Phase 11's input guards. Building and testing them separately from the wiring step ensures each guard is correct in isolation before integration.

Output: Two tested utility modules (`src/utils/validation.ts`, `src/utils/rate-limiter.ts`) and the `rate-limiter-flexible` production dependency installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-input-validation-safety-limits/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/errors.ts:
```typescript
export class ValidationError extends MailMCPError {
  constructor(message: string, options?: ErrorOptions) {
    super(MailErrorCode.ValidationError, message, options);
  }
}

export class QuotaError extends MailMCPError {
  constructor(message: string, options?: ErrorOptions) {
    super(MailErrorCode.QuotaError, message, options);
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Email validation utility with tests</name>
  <files>src/utils/validation.ts, src/utils/validation.test.ts</files>
  <read_first>src/errors.ts, src/utils/markdown.ts</read_first>
  <behavior>
    - `validateEmailAddresses("notanemail")` throws ValidationError with message containing "notanemail"
    - `validateEmailAddresses("user@example.com")` does not throw
    - `validateEmailAddresses("Name <user@example.com>")` does not throw (angle-bracket format)
    - `validateEmailAddresses("a@b.com, notvalid, c@d.com")` throws ValidationError listing "notvalid"
    - `validateEmailAddresses(undefined)` does not throw (optional fields)
    - `validateEmailAddresses("a@b.com", undefined, "c@d.com")` does not throw (mixed present/absent fields)
    - `validateEmailAddresses("user @example.com")` throws ValidationError (whitespace in local part)
    - `validateEmailAddresses("")` does not throw (empty string = no addresses)
  </behavior>
  <action>
    1. Create `src/utils/validation.test.ts` with tests for all behaviors listed above. Import `ValidationError` from `../errors.js`. Import `validateEmailAddresses` from `./validation.js`. Use `describe`/`it`/`expect` from vitest. Test that thrown errors are instances of `ValidationError`.

    2. Run tests — confirm they FAIL (RED).

    3. Create `src/utils/validation.ts`:
       - Define `const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;` (ReDoS-safe — no nested quantifiers)
       - Export function `validateEmailAddresses(...addrFields: Array<string | undefined>): void`
       - For each non-undefined, non-empty field: split on commas, trim each entry
       - For each entry: extract angle-bracket address via `/<([^>]+)>/` match, fall back to raw trimmed string
       - Skip empty strings after trim (handles trailing commas)
       - Test extracted email against `EMAIL_RE`; collect invalid entries
       - If any invalid: `throw new ValidationError('Invalid email address(es): ' + invalid.join(', '))`
       - Import `ValidationError` from `'../errors.js'`

    4. Run tests — confirm they PASS (GREEN).

    Anti-patterns to avoid:
    - Do NOT use a full RFC 5322 regex (ReDoS risk — H-09 pitfall)
    - Do NOT install any npm package for email validation (zero-dependency regex is sufficient)
  </action>
  <verify>
    <automated>cd /Users/mis/dev/mail_mcp && npx vitest run src/utils/validation.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "EMAIL_RE" src/utils/validation.ts` succeeds
    - `grep -q "validateEmailAddresses" src/utils/validation.ts` succeeds
    - `grep -q "ValidationError" src/utils/validation.ts` succeeds
    - `grep -q "Invalid email address" src/utils/validation.ts` succeeds
    - `npx vitest run src/utils/validation.test.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>validateEmailAddresses function exists, handles all edge cases (angle brackets, comma-separated, undefined fields, empty strings), throws ValidationError for invalid addresses, all unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-account rate limiter with tests</name>
  <files>src/utils/rate-limiter.ts, src/utils/rate-limiter.test.ts, package.json</files>
  <read_first>src/errors.ts, package.json</read_first>
  <behavior>
    - `AccountRateLimiter.consume("acct1")` resolves without error when under limit
    - After consuming all 100 points for "acct1", next `consume("acct1")` throws QuotaError with message containing "Rate limit exceeded" and "acct1"
    - After exhausting "acct1", `consume("acct2")` still resolves (per-account isolation)
    - QuotaError message contains "Retry after" with a seconds value
    - Constructor accepts custom `points` and `duration` for testing with small limits
  </behavior>
  <action>
    1. Install `rate-limiter-flexible`:
       ```
       npm install rate-limiter-flexible
       ```
       Verify installation: `npx tsc --noEmit` should pass (confirms ESM/TypeScript compatibility with `"moduleResolution": "NodeNext"`).

    2. Create `src/utils/rate-limiter.test.ts` with tests for all behaviors. Use a small limit (e.g., `points: 3, duration: 60`) for fast tests. Import `QuotaError` from `../errors.js`. Import `AccountRateLimiter` from `./rate-limiter.js`. Verify thrown errors are instances of `QuotaError`. For per-account isolation: exhaust account A (consume 3+1 times), verify account B still succeeds.

    3. Run tests — confirm they FAIL (RED).

    4. Create `src/utils/rate-limiter.ts`:
       - `import { RateLimiterMemory, RateLimiterRes } from 'rate-limiter-flexible';`
       - `import { QuotaError } from '../errors.js';`
       - Export `const DEFAULT_RATE_LIMIT_POINTS = 100;`
       - Export `const DEFAULT_RATE_LIMIT_DURATION = 60;`
       - Export `class AccountRateLimiter`:
         - Private `limiters = new Map<string, RateLimiterMemory>()`
         - Constructor takes optional `points` (default `DEFAULT_RATE_LIMIT_POINTS`) and `duration` (default `DEFAULT_RATE_LIMIT_DURATION`)
         - Private `getLimiter(accountId: string): RateLimiterMemory` — lazy-creates per account
         - Public `async consume(accountId: string): Promise<void>` — calls `this.getLimiter(accountId).consume(accountId)`, catches rejection. If `res instanceof RateLimiterRes`, compute `Math.ceil(res.msBeforeNext / 1000)` for retry seconds. Throw `new QuotaError('Rate limit exceeded for account "${accountId}". Retry after ${wait} second(s).')`

    5. Run tests — confirm they PASS (GREEN).

    Anti-patterns to avoid:
    - Do NOT create a module-level singleton (H-05 pitfall). The class must be instantiated per MailMCPServer.
    - Do NOT use a single RateLimiterMemory for all accounts. Use Map<accountId, RateLimiterMemory> for per-account isolation.
  </action>
  <verify>
    <automated>cd /Users/mis/dev/mail_mcp && npx vitest run src/utils/rate-limiter.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rate-limiter-flexible" package.json` succeeds
    - `grep -q "AccountRateLimiter" src/utils/rate-limiter.ts` succeeds
    - `grep -q "QuotaError" src/utils/rate-limiter.ts` succeeds
    - `grep -q "DEFAULT_RATE_LIMIT_POINTS" src/utils/rate-limiter.ts` succeeds
    - `grep -q "Map<string, RateLimiterMemory>" src/utils/rate-limiter.ts` succeeds (per-account Map pattern)
    - `npx vitest run src/utils/rate-limiter.test.ts` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0 (TypeScript compilation clean)
  </acceptance_criteria>
  <done>AccountRateLimiter class exists with per-account Map pattern, throws QuotaError on limit exceeded, rate-limiter-flexible installed, all unit tests pass, TypeScript compiles clean</done>
</task>

</tasks>

<verification>
```bash
# All new tests pass
npx vitest run src/utils/validation.test.ts src/utils/rate-limiter.test.ts --reporter=verbose

# Full test suite still passes (no regressions)
npm test

# TypeScript compiles
npx tsc --noEmit
```
</verification>

<success_criteria>
- `src/utils/validation.ts` exports `validateEmailAddresses` using ReDoS-safe regex
- `src/utils/rate-limiter.ts` exports `AccountRateLimiter` with per-account `Map<string, RateLimiterMemory>`
- `rate-limiter-flexible` added to package.json dependencies
- All new unit tests pass
- Full `npm test` suite passes without regressions
- `npx tsc --noEmit` compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/11-input-validation-safety-limits/11-01-SUMMARY.md`
</output>
