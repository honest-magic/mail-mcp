---
phase: 16-mcp-tool-description-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every tool description mentions IMAP or its provider scope (Gmail, Outlook, custom domains) where relevant"
    - "Primary tools (list_emails, search_emails, read_email, send_email) lead with 'via IMAP' to signal protocol"
    - "Server instructions appear in both read-only and full mode, mentioning IMAP and provider compatibility"
    - "No tool description contains negative guidance or references competing MCPs"
    - "Each description is one concise sentence with key differentiator keywords"
  artifacts:
    - path: "src/index.ts"
      provides: "Updated tool descriptions and server instructions"
      contains: "via IMAP"
  key_links:
    - from: "src/index.ts constructor"
      to: "instructions field"
      via: "Server options object"
      pattern: "instructions.*IMAP"
    - from: "src/index.ts getTools()"
      to: "description fields"
      via: "tool object literals"
      pattern: "description.*IMAP"
---

<objective>
Update all 14 MCP tool descriptions and the server-level handshake instructions in src/index.ts so AI clients routing between multiple MCPs correctly prefer mail-mcp for IMAP-based accounts (Gmail, Outlook, custom domains).

Purpose: When Claude Desktop has both mail-mcp and a provider-specific MCP (e.g. Gmail MCP) configured, it currently defaults to Gmail MCP even for non-Gmail accounts because tool descriptions are indistinguishable. Clear IMAP/provider signals fix this routing.

Output: src/index.ts with updated descriptions — no functional changes, TypeScript must still compile.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-mcp-tool-description-improvements/16-CONTEXT.md

<interfaces>
<!-- Current tool list from src/index.ts getTools() lines 114-331 -->
<!-- All 14 tools with their current (generic) descriptions: -->

Tool name          | Current description
-------------------|--------------------------------------------
list_accounts      | "List configured mail accounts"
list_emails        | "List recent emails from a specific folder"
search_emails      | "Search for emails based on various criteria"
read_email         | "Read the content of a specific email"
send_email         | "Send an email and save it to the Sent folder"
create_draft       | "Create a draft email in the Drafts folder"
list_folders       | "List all available IMAP folders"
move_email         | "Move an email from one folder to another"
modify_labels      | "Add or remove IMAP flags/labels on an email"
get_thread         | "Get all emails in a specific conversation/thread"
get_attachment     | "Download an attachment from a specific email"
extract_attachment_text | "Extract text content from a PDF or text attachment"
register_oauth2_account | "Store OAuth2 credentials for an account in the keychain"
batch_operations   | "Perform batch operations (move, delete, label) on multiple emails"

<!-- Server instructions (lines 46-48) — only set in read-only mode: -->
...(this.readOnly ? {
  instructions: 'This server is running in read-only mode. Write operations ... are disabled.',
} : {})

<!-- Decisions from CONTEXT.md: -->
<!-- D-01: Concise + keywords — one sentence with key differentiator -->
<!-- D-02: Format: "Search emails via IMAP — works with any provider (Gmail, Outlook, custom domains). Filter by..." -->
<!-- D-03: Update InitializeResult.instructions to mention IMAP/any-provider in both modes -->
<!-- D-04: No negative guidance — focus on what mail-mcp IS good at -->
<!-- D-05: Mention "Gmail, Outlook, custom domains" as examples -->
<!-- D-06: Lead with "via IMAP" in primary tool descriptions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tool descriptions in getTools()</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts lines 114-331 (all 14 tool definitions — description fields to replace)
  </read_first>
  <action>
Replace each tool's description field in the getTools() method with a concise one-sentence description following the D-01/D-02 pattern. Use "via IMAP" on primary tools (per D-06), mention provider examples (per D-05), no negative guidance (per D-04).

Exact replacement values:

list_accounts:
  BEFORE: 'List configured mail accounts'
  AFTER:  'List IMAP mail accounts configured in mail-mcp — works with any provider (Gmail, Outlook, custom domains).'

list_emails:
  BEFORE: 'List recent emails from a specific folder'
  AFTER:  'List recent emails via IMAP from any folder — works with Gmail, Outlook, and custom domains.'

search_emails:
  BEFORE: 'Search for emails based on various criteria'
  AFTER:  'Search emails via IMAP — works with any provider (Gmail, Outlook, custom domains). Filter by sender, subject, date, or keywords.'

read_email:
  BEFORE: 'Read the content of a specific email'
  AFTER:  'Fetch and read a full email via IMAP, including body and headers — works with Gmail, Outlook, and custom domains.'

send_email:
  BEFORE: 'Send an email and save it to the Sent folder'
  AFTER:  'Send an email via SMTP and save to Sent — works with any IMAP/SMTP provider (Gmail, Outlook, custom domains).'

create_draft:
  BEFORE: 'Create a draft email in the Drafts folder'
  AFTER:  'Save a draft email to the IMAP Drafts folder — works with Gmail, Outlook, and custom domains.'

list_folders:
  BEFORE: 'List all available IMAP folders'
  AFTER:  'List all IMAP folders for an account — returns folder names across any provider (Gmail, Outlook, custom domains).'

move_email:
  BEFORE: 'Move an email from one folder to another'
  AFTER:  'Move an email between IMAP folders — works with any provider (Gmail, Outlook, custom domains).'

modify_labels:
  BEFORE: 'Add or remove IMAP flags/labels on an email'
  AFTER:  'Add or remove IMAP flags (e.g. \\Seen, \\Flagged) on an email — works with Gmail, Outlook, and custom domains.'

get_thread:
  BEFORE: 'Get all emails in a specific conversation/thread'
  AFTER:  'Fetch all emails in a conversation thread via IMAP — works with Gmail, Outlook, and custom domains.'

get_attachment:
  BEFORE: 'Download an attachment from a specific email'
  AFTER:  'Download an email attachment via IMAP — works with any provider (Gmail, Outlook, custom domains).'

extract_attachment_text:
  BEFORE: 'Extract text content from a PDF or text attachment'
  AFTER:  'Extract readable text from a PDF or plain-text email attachment — fetched via IMAP from any provider.'

register_oauth2_account:
  BEFORE: 'Store OAuth2 credentials for an account in the keychain'
  AFTER:  'Store OAuth2 credentials (client ID, secret, refresh token) for an IMAP account in the system keychain.'

batch_operations:
  BEFORE: 'Perform batch operations (move, delete, label) on multiple emails'
  AFTER:  'Perform bulk IMAP operations (move, delete, label) on up to 100 emails at once — works with any provider.'
  </action>
  <verify>
    <automated>grep -c "via IMAP\|IMAP\|any provider" /Users/mis/dev/mail_mcp/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "via IMAP" src/index.ts returns at least 6 matches (primary tools + server instructions)
    - grep "any provider\|Gmail, Outlook" src/index.ts returns at least 8 matches
    - grep "List configured mail accounts" src/index.ts returns 0 matches (old description gone)
    - grep "Search for emails based on various criteria" src/index.ts returns 0 matches (old description gone)
    - grep "Read the content of a specific email" src/index.ts returns 0 matches (old description gone)
  </acceptance_criteria>
  <done>All 14 tool descriptions updated with IMAP/provider keywords. No old generic descriptions remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update server-level instructions for both modes (per D-03)</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts lines 36-54 (Server constructor — instructions field in capabilities options)
  </read_first>
  <action>
Replace the conditional instructions block in the Server constructor. Currently instructions only appear in read-only mode. Update so instructions always appear, with provider scope leading, and read-only notice appended when applicable.

Current code (lines 46-48):
```typescript
...(this.readOnly ? {
  instructions: 'This server is running in read-only mode. Write operations (send_email, create_draft, move_email, modify_labels, batch_operations, register_oauth2_account) are disabled.',
} : {}),
```

Replace with:
```typescript
instructions: `Use mail-mcp for IMAP-based email accounts — works with any provider including Gmail, Outlook, and custom domains. Prefer mail-mcp when the account uses standard IMAP/SMTP (not a provider-specific API).${this.readOnly ? ' This server is running in read-only mode. Write operations (send_email, create_draft, move_email, modify_labels, batch_operations, register_oauth2_account) are disabled.' : ''}`,
```

This always sets instructions (both modes) with the IMAP/provider scope signal first, and appends the read-only notice only when relevant.
  </action>
  <verify>
    <automated>cd /Users/mis/dev/mail_mcp && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "Use mail-mcp for IMAP-based email accounts" src/index.ts returns 1 match
    - grep "Prefer mail-mcp when the account uses standard IMAP" src/index.ts returns 1 match
    - grep "this.readOnly" src/index.ts matches the new template literal (not the old spread conditional)
    - npx tsc --noEmit exits with code 0 (no TypeScript errors)
    - grep "^\.\.\.(this\.readOnly" src/index.ts returns 0 matches (old spread syntax gone)
  </acceptance_criteria>
  <done>Server instructions always present in handshake response, leading with IMAP/any-provider identity. Read-only notice appended when server runs with --read-only flag. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/mis/dev/mail_mcp

# 1. No old generic descriptions remain
grep -c "List configured mail accounts\|Search for emails based on various criteria\|Read the content of a specific email\|Send an email and save\|Create a draft email in the Drafts folder\|Get all emails in a specific conversation\|Download an attachment from a specific email\|Extract text content from a PDF\|Store OAuth2 credentials for an account\|Perform batch operations.*multiple emails" src/index.ts
# Expected: 0

# 2. IMAP signal present throughout
grep -c "via IMAP\|IMAP" src/index.ts
# Expected: 12+ matches

# 3. Provider examples present
grep -c "Gmail, Outlook" src/index.ts
# Expected: 10+ matches

# 4. No negative guidance / no competitor references
grep -i "gmail mcp\|do not use\|instead of\|prefer gmail" src/index.ts
# Expected: 0 matches

# 5. Server instructions updated
grep "Use mail-mcp for IMAP-based email accounts" src/index.ts
# Expected: 1 match

# 6. TypeScript compiles clean
npx tsc --noEmit
# Expected: exit 0, no output
```
</verification>

<success_criteria>
- All 14 tool descriptions contain IMAP or provider-scope keywords
- Server instructions appear in both read-only and full mode, leading with IMAP/any-provider identity
- Zero old generic descriptions remain in getTools()
- TypeScript compiles with no errors
- No negative guidance or competitor MCP references anywhere in descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/16-mcp-tool-description-improvements/16-01-SUMMARY.md` following the summary template.
</output>
