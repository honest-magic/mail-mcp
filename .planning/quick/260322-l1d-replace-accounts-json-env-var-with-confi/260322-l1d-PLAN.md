---
phase: quick-260322-l1d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/index.ts
  - src/index.test.ts
  - README.md
autonomous: true
requirements: [QUICK-L1D]
must_haves:
  truths:
    - "Server reads account config from ~/.config/mail-mcp/accounts.json on startup"
    - "ACCOUNTS_JSON env var is no longer used or referenced"
    - "Keychain remains the sole credential store (unchanged)"
    - "list_accounts tool returns accounts loaded from the config file"
    - "README documents the new config file approach instead of env var"
  artifacts:
    - path: "src/config.ts"
      provides: "getAccounts() reads from ~/.config/mail-mcp/accounts.json"
      contains: "accounts.json"
    - path: "README.md"
      provides: "Updated configuration documentation"
      contains: "~/.config/mail-mcp/accounts.json"
  key_links:
    - from: "src/index.ts"
      to: "src/config.ts"
      via: "import { getAccounts }"
      pattern: "getAccounts"
    - from: "src/config.ts"
      to: "~/.config/mail-mcp/accounts.json"
      via: "fs.readFileSync"
      pattern: "accounts\\.json"
---

<objective>
Replace the ACCOUNTS_JSON environment variable with file-based config: server reads account definitions from ~/.config/mail-mcp/accounts.json on startup. Keychain remains unchanged for credentials.

Purpose: Eliminate the awkward env var (especially painful in JSON-in-JSON MCP client configs) in favor of a standard config file.
Output: Updated config.ts, index.ts, tests, and README.
</objective>

<context>
@src/config.ts
@src/types/index.ts
@src/index.ts
@src/index.test.ts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace getAccounts() to read from config file</name>
  <files>src/config.ts</files>
  <action>
Rewrite `getAccounts()` in src/config.ts to:

1. Build the config path: `path.join(os.homedir(), '.config', 'mail-mcp', 'accounts.json')` using `node:path` and `node:os`.
2. Check if the file exists using `fs.existsSync()`. If not, log a warning to stderr: `"No accounts config found at ~/.config/mail-mcp/accounts.json"` and return `[]`.
3. Read the file with `fs.readFileSync(configPath, 'utf-8')`.
4. Parse JSON and validate it is an array. Return as `EmailAccount[]`.
5. Wrap in try/catch — on parse error, log `"Failed to parse accounts config at {path}: {error}"` to stderr and return `[]`.

Remove all references to `process.env.ACCOUNTS_JSON`. Also remove the `dotenv` import and `dotenv.config()` call since ACCOUNTS_JSON was the only env var it served (SERVICE_NAME and LOG_LEVEL have defaults and are not user-facing). Actually, keep dotenv if SERVICE_NAME or LOG_LEVEL might be set by users — but review: these are internal config with defaults. Remove dotenv entirely since the config file replaces the only user-facing env var. The serviceName and logLevel can stay with their z.object defaults.

Use `node:fs`, `node:path`, `node:os` (built-in, no new deps).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>getAccounts() reads from ~/.config/mail-mcp/accounts.json, no references to ACCOUNTS_JSON remain in config.ts, dotenv removed</done>
</task>

<task type="auto">
  <name>Task 2: Update tests and list_accounts references</name>
  <files>src/index.test.ts, src/index.ts</files>
  <action>
**src/index.ts:** No code changes needed to index.ts itself — it already imports `getAccounts` from `./config.js` and calls it. The interface is unchanged. Just confirm `getAccounts` is still called the same way (it is — the function signature and return type are identical).

**src/index.test.ts:** Update the mock for `./config.js`:
- The existing mock `vi.mock('./config.js', ...)` already mocks `getAccounts` — this continues to work as-is since the function signature is unchanged.
- No test changes needed for the mock pattern.

However, add one new test in a new describe block `'config file loading'` that validates the getAccounts behavior directly (not through the mock). This should be in a separate test file `src/config.test.ts`:

Create `src/config.test.ts`:
- Mock `node:fs` and `node:os` using vi.mock
- Test 1: "returns empty array when config file does not exist" — mock existsSync to return false, verify getAccounts() returns []
- Test 2: "parses valid accounts.json" — mock existsSync true, readFileSync returns a valid JSON array of one EmailAccount, verify getAccounts() returns the parsed array
- Test 3: "returns empty array on invalid JSON" — mock existsSync true, readFileSync returns invalid JSON, verify getAccounts() returns [] and does not throw

Update files_modified: also creates src/config.test.ts.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>All existing tests pass, new config.test.ts tests pass, no ACCOUNTS_JSON references in test files</done>
</task>

<task type="auto">
  <name>Task 3: Update README for config file approach</name>
  <files>README.md</files>
  <action>
Update README.md section "## Configuration":

**Section "### 1. Define accounts":**
Replace the ACCOUNTS_JSON env var instructions with:

```
Create `~/.config/mail-mcp/accounts.json`:

```bash
mkdir -p ~/.config/mail-mcp
cat > ~/.config/mail-mcp/accounts.json << 'EOF'
[
  {
    "id": "work",
    ...same example as before...
  }
]
EOF
```

Keep the existing fields table unchanged.

**Section "### 3. Add to your MCP client":**
Remove the `"env": { "ACCOUNTS_JSON": "..." }` blocks from both Claude Desktop and Generic MCP client examples. The config examples simplify to just command + args (no env block needed for accounts).

Update Claude Desktop example:
```json
{
  "mcpServers": {
    "mail": {
      "command": "npx",
      "args": ["-y", "@honest-magic/mail-mcp"]
    }
  }
}
```

Update Generic MCP client example similarly (remove env block).

**Read-only mode section:** Update the read-only Claude Desktop config example to also remove the `"env": { "ACCOUNTS_JSON": "..." }` block.

Do NOT change anything in sections 2 (Store credentials), Available Tools, Read-Only Mode description text, or License.
  </action>
  <verify>
    <automated>grep -r "ACCOUNTS_JSON" README.md src/ ; test $? -eq 1</automated>
  </verify>
  <done>README documents ~/.config/mail-mcp/accounts.json, no ACCOUNTS_JSON references remain anywhere in src/ or README.md</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles without errors
2. `npx vitest run` — All tests pass (existing + new config tests)
3. `grep -r "ACCOUNTS_JSON" src/ README.md` — Returns no matches
4. `grep -r "dotenv" src/config.ts` — Returns no matches
</verification>

<success_criteria>
- getAccounts() reads from ~/.config/mail-mcp/accounts.json (not env var)
- Keychain credential storage is completely untouched
- All tests pass
- No ACCOUNTS_JSON references remain in source or docs
- README shows the simpler config file setup
</success_criteria>

<output>
After completion, create `.planning/quick/260322-l1d-replace-accounts-json-env-var-with-confi/260322-l1d-SUMMARY.md`
</output>
