---
phase: 03-context-resources
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/protocol/imap.ts, src/services/mail.ts, src/index.ts, package.json]
autonomous: true
requirements: [RES-01, RES-02, RES-03]

must_haves:
  truths:
    - "AI can see metadata of email attachments without downloading them"
    - "AI can explicitly download attachment content for analysis"
    - "AI can extract text from PDF attachments for reasoning"
  artifacts:
    - path: "src/services/mail.ts"
      provides: "readEmail with attachment metadata, downloadAttachment operation"
    - path: "src/index.ts"
      provides: "get_attachment, extract_attachment_text MCP tools"
  key_links:
    - from: "src/index.ts"
      to: "pdf-parse"
      via: "PDF text extraction logic"
---

<objective>
Enable handling of email attachments through metadata listings, on-demand downloads via MCP Resource URIs/Tools, and automated text extraction from PDFs.

Purpose: Allow the AI to access and reason over supplementary files sent via email.
Output: Attachment metadata in `read_email`, and two new tools: `get_attachment` and `extract_attachment_text`.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/protocol/imap.ts
@src/services/mail.ts
@.planning/phases/03-context-resources/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attachment metadata in readEmail and Tool support</name>
  <files>src/services/mail.ts, src/index.ts</files>
  <action>
    - Update `MailService.readEmail` to return a list of attachments: `{ filename, contentType, size, contentId }`.
    - Implement `downloadAttachment(uid: string, filename: string, folder: string)` in `MailService`.
    - It should fetch the email, find the attachment by filename, and return the raw buffer (or base64).
    - Register `get_attachment` tool in `src/index.ts`.
  </action>
  <verify>
    <automated>npm test src/services/mail.test.ts</automated>
  </verify>
  <done>Attachments are listed in message views and can be downloaded as raw data.</done>
</task>

<task type="auto">
  <name>Task 2: PDF and Text Extraction Tool</name>
  <files>src/index.ts, package.json</files>
  <action>
    - Install `pdf-parse` library via npm.
    - Implement `extract_attachment_text` tool in `src/index.ts`.
    - For PDFs: Use `pdf-parse` to return text.
    - For text files: Return the content directly.
    - Handle large content by providing a concise summary if it exceeds token limits (or simply return it, as MCP handles reasonably large text).
  </action>
  <verify>
    <automated>npm run build && node dist/index.js (with mock data if needed)</automated>
  </verify>
  <done>AI can reason over the textual content of PDF and document attachments.</done>
</task>

</tasks>

<verification>
Verify that an email with a PDF attachment lists the PDF, and calling `extract_attachment_text` returns the expected text from the PDF.
</verification>

<success_criteria>
1. Metadata for attachments is visible in `read_email` output.
2. `get_attachment` tool returns base64 data for binary files.
3. `extract_attachment_text` successfully extracts text from PDFs.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-resources/03-02-SUMMARY.md`
</output>
