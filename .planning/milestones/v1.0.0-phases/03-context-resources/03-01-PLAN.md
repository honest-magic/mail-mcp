---
phase: 03-context-resources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/protocol/imap.ts, src/services/mail.ts, src/index.ts]
autonomous: true
requirements: [THRD-01, THRD-02, THRD-03]

must_haves:
  truths:
    - "User can fetch all messages belonging to a conversation thread"
    - "Threaded messages are returned in chronological order"
    - "Conversation view is token-efficient and easy for LLMs to reason over"
  artifacts:
    - path: "src/protocol/imap.ts"
      provides: "Header-based thread reconstruction"
    - path: "src/services/mail.ts"
      provides: "MailService.getThread operation"
  key_links:
    - from: "src/index.ts"
      to: "MailService.getThread"
      via: "get_thread MCP tool"
---

<objective>
Implement robust thread reconstruction using IMAP headers and provide a conversation view for the AI to reason over full message histories.

Purpose: Enable the AI to understand the context of a conversation by seeing all related messages.
Output: A new `get_thread` tool that returns a list of related messages.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/protocol/imap.ts
@src/services/mail.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header-based thread discovery in ImapClient</name>
  <files>src/protocol/imap.ts</files>
  <action>
    - Add `fetchThreadMessages(uid: string, folder: string)` to `ImapClient`.
    - First, fetch the target message's `Message-ID`, `In-Reply-To`, and `References` headers.
    - If the server supports `threadId` (Gmail/X-GM-EXT-1), use it for high performance.
    - Fallback: Search for messages that reference this Message-ID or are referenced by this message.
    - Combine results and return `MessageMetadata[]`.
  </action>
  <verify>
    <automated>npm test src/protocol/imap.test.ts</automated>
  </verify>
  <done>ImapClient can retrieve a list of related message metadata using headers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement get_thread Tool and MailService operation</name>
  <files>src/services/mail.ts, src/index.ts</files>
  <action>
    - Add `getThread(uid: string, folder: string)` to `MailService`.
    - It should fetch all messages in the thread, resolve their full bodies (or snippets), and return a sorted list of messages.
    - Format output for the LLM: `[DATE] FROM: ... SUBJECT: ... BODY: ...` for each message.
    - Register the `get_thread` tool in `src/index.ts`.
  </action>
  <verify>
    <automated>npm test src/sanity.test.ts</automated>
  </verify>
  <done>The get_thread tool successfully returns a conversation context for a given email UID.</done>
</task>

</tasks>

<verification>
Verify that fetching a thread for an email with replies returns all related messages in chronological order.
</verification>

<success_criteria>
1. Tool `get_thread` registered and functional.
2. Thread reconstruction works across basic IMAP servers using References/In-Reply-To.
3. Chronological sorting maintained.
</success_criteria>

<output>
After completion, create `.planning/phases/03-context-resources/03-01-SUMMARY.md`
</output>
