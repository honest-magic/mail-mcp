---
phase: 02-discovery-organization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/protocol/imap.ts, src/protocol/smtp.ts, src/services/mail.ts, src/index.ts]
autonomous: true
requirements: [IMAP-03, SMTP-02]

must_haves:
  truths:
    - "User can search for emails by sender, subject, and date range"
    - "User can send emails with CC and BCC recipients"
  artifacts:
    - path: "src/protocol/imap.ts"
      provides: "ImapClient.searchMessages method"
    - path: "src/protocol/smtp.ts"
      provides: "SmtpClient.send with CC/BCC support"
    - path: "src/index.ts"
      provides: "search_emails tool registration"
  key_links:
    - from: "src/index.ts"
      to: "src/services/mail.ts"
      via: "tool handler"
    - from: "src/services/mail.ts"
      to: "src/protocol/imap.ts"
      via: "searchMessages call"
---

<objective>
Implement advanced search capabilities for IMAP and enhance SMTP sending to support CC/BCC recipients.

Purpose: Enable users to find specific information across their mailbox and handle more complex communication patterns.
Output: New `search_emails` tool and enhanced `send_email` tool.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/protocol/imap.ts
@src/protocol/smtp.ts
@src/services/mail.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement advanced search</name>
  <files>src/protocol/imap.ts, src/services/mail.ts, src/index.ts</files>
  <action>
    Implement `searchMessages` in `ImapClient` using `imapflow.search()`. 
    The search should support criteria like `from`, `subject`, `since`, `before`, and keywords.
    Update `MailService` to expose this functionality.
    Register the `search_emails` tool in `src/index.ts` with a schema that accepts these search parameters.
    Default limit should be 10 messages, max 50 per phase context.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    `search_emails` tool is registered and returns filtered messages based on criteria.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CC/BCC support to SMTP</name>
  <files>src/protocol/smtp.ts, src/services/mail.ts, src/index.ts</files>
  <action>
    Update `SmtpClient.send` to accept optional `cc` and `bcc` parameters (strings or arrays).
    Update `MailService.sendEmail` to pass these parameters to the client.
    Update `send_email` tool registration in `src/index.ts` to include optional `cc` and `bcc` fields in the input schema.
    Ensure `MailService.createDraft` and `appendMessage` (Sent folder sync) also include these headers in the raw message if possible.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    `send_email` tool supports CC and BCC recipients.
  </done>
</task>

</tasks>

<verification>
Check if `search_emails` works with different filters.
Check if `send_email` correctly delivers to CC/BCC recipients (by checking SMTP logs or sent folder).
</verification>

<success_criteria>
1. `search_emails` tool returns expected messages.
2. `send_email` tool accepts and uses CC/BCC fields.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-organization/02-01-SUMMARY.md`
</output>
