---
phase: 02-discovery-organization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/protocol/imap.ts, src/services/mail.ts, src/index.ts]
autonomous: true
requirements: [IMAP-04, ORG-01, ORG-02]

must_haves:
  truths:
    - "User can list folders and labels"
    - "User can move emails between folders"
    - "User can add and remove labels from emails"
  artifacts:
    - path: "src/protocol/imap.ts"
      provides: "ImapClient methods for folder and label management"
    - path: "src/index.ts"
      provides: "list_folders, move_email, and modify_labels tools"
  key_links:
    - from: "src/index.ts"
      to: "src/services/mail.ts"
      via: "tool handlers"
---

<objective>
Implement mailbox structure and organization tools: folder listing, message moving, and label management.

Purpose: Allow users to organize their inbox by categorizing and filing messages.
Output: New `list_folders`, `move_email`, and `modify_labels` tools.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/protocol/imap.ts
@src/services/mail.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Folder listing and management</name>
  <files>src/protocol/imap.ts, src/services/mail.ts, src/index.ts</files>
  <action>
    Implement `listFolders` in `ImapClient` using `imapflow.list()`. 
    Update `MailService` to expose this functionality.
    Register `list_folders` tool in `src/index.ts`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    `list_folders` tool returns a tree or list of folders available on the server.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move and label emails</name>
  <files>src/protocol/imap.ts, src/services/mail.ts, src/index.ts</files>
  <action>
    Implement `moveMessage` and `modifyLabels` in `ImapClient`. 
    Use `imapflow.move()` for `moveMessage`.
    For `modifyLabels`, use `imapflow.setFlags()` to add or remove keywords (flags).
    If it's Gmail, consider supporting `X-GM-LABELS` extension if `imapflow` provides it.
    Update `MailService` to expose these methods.
    Register `move_email` and `modify_labels` tools in `src/index.ts`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    `move_email` and `modify_labels` tools correctly organize messages on the IMAP server.
  </done>
</task>

</tasks>

<verification>
Check if `list_folders` returns accurate folder structure.
Check if moving an email reflects correctly in the destination folder.
Check if adding/removing labels correctly persists as IMAP flags or keywords.
</verification>

<success_criteria>
1. `list_folders` correctly lists all folders.
2. `move_email` correctly relocates messages.
3. `modify_labels` correctly applies or removes labels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-organization/02-02-SUMMARY.md`
</output>
