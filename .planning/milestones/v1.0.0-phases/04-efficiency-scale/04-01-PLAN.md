---
phase: 04-efficiency-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/protocol/imap.ts, src/services/mail.ts, src/index.ts]
autonomous: true
requirements: [ORG-03]

must_haves:
  truths:
    - "User can move multiple emails in a single request"
    - "User can delete multiple emails in a single request"
    - "User can add/remove labels for multiple emails in a single request"
  artifacts:
    - path: "src/protocol/imap.ts"
      provides: "Batch IMAP operations"
    - path: "src/services/mail.ts"
      provides: "MailService batch coordination"
    - path: "src/index.ts"
      provides: "batch_operations MCP tool"
  key_links:
    - from: "src/index.ts"
      to: "src/services/mail.ts"
      via: "batch_operations tool handler"
    - from: "src/services/mail.ts"
      to: "src/protocol/imap.ts"
      via: "batchMove, batchDelete, batchModifyLabels calls"
---

<objective>
Implement batch operations for moving, deleting, and labeling multiple emails in a single request to improve efficiency for high-volume tasks.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/protocol/imap.ts
@src/services/mail.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch methods to ImapClient</name>
  <files>src/protocol/imap.ts</files>
  <action>
    Implement the following methods in `ImapClient` class:
    - `batchMoveMessages(uids: string[], sourceFolder: string, targetFolder: string): Promise<void>`
    - `batchDeleteMessages(uids: string[], folder: string): Promise<void>`
    - `batchModifyLabels(uids: string[], folder: string, addLabels: string[], removeLabels: string[]): Promise<void>`
    
    Use `uids.join(',')` to pass as a sequence to `imapflow` methods like `messageMove`, `messageDelete`, `messageFlagsAdd`, and `messageFlagsRemove`.
    Ensure proper mailbox locking for each operation.
  </action>
  <verify>
    Check that the new methods are present and correctly use `imapflow` sequences.
  </verify>
  <done>
    `ImapClient` supports batch move, delete, and label modification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add batch operations to MailService</name>
  <files>src/services/mail.ts</files>
  <action>
    Implement `batchOperations` in `MailService`:
    - It should accept `uids: string[]`, `folder: string`, and an `operation` object.
    - `operation` can be `{ type: 'move', targetFolder: string }`, `{ type: 'delete' }`, or `{ type: 'label', addLabels?: string[], removeLabels?: string[] }`.
    - Limit the number of UIDs to 100 as per D-01 in Phase 4 context.
    - Call the corresponding `ImapClient` batch methods.
  </action>
  <verify>
    Check that `MailService` correctly routes batch operations and enforces the 100-UID limit.
  </verify>
  <done>
    `MailService` exposes a unified `batchOperations` method.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register batch_operations tool</name>
  <files>src/index.ts</files>
  <action>
    Register the `batch_operations` tool in `src/index.ts`:
    - `name`: `batch_operations`
    - `description`: `Perform batch operations (move, delete, label) on multiple emails`
    - `inputSchema`:
      - `accountId`: string (required)
      - `uids`: string[] (required)
      - `folder`: string (required)
      - `action`: string (required, one of: 'move', 'delete', 'label')
      - `targetFolder`: string (optional, for 'move')
      - `addLabels`: string[] (optional, for 'label')
      - `removeLabels`: string[] (optional, for 'label')
    
    Implement the handler to call `service.batchOperations`.
  </action>
  <verify>
    Run `npm run build` and ensure no compilation errors.
  </verify>
  <done>
    The `batch_operations` tool is available via MCP.
  </done>
</task>

</tasks>

<verification>
- Verify `batch_operations` with 'move' action moves multiple UIDs.
- Verify `batch_operations` with 'delete' action deletes multiple UIDs.
- Verify `batch_operations` with 'label' action modifies flags for multiple UIDs.
- Verify 100-UID limit enforcement.
</verification>

<success_criteria>
- User can perform batch operations on up to 100 emails in a single tool call.
- Supported operations: move, delete, add labels, remove labels.
</success_criteria>

<output>
After completion, create `.planning/phases/04-efficiency-scale/04-01-SUMMARY.md`
</output>
