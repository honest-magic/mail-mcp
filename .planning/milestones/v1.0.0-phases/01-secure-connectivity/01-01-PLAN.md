---
phase: 01-secure-connectivity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, src/index.ts, src/types/index.ts, src/security/keychain.ts, src/config.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Credentials can be securely stored and retrieved from macOS Keychain"
    - "MCP server starts and lists available tools"
  artifacts:
    - path: "src/security/keychain.ts"
      provides: "Secure credential storage"
    - path: "src/index.ts"
      provides: "MCP Server entry point"
  key_links:
    - from: "src/index.ts"
      to: "src/security/keychain.ts"
      via: "imports"
---

<objective>
Set up the project foundation, including the TypeScript environment, MCP server boilerplate, and secure credential storage using macOS Keychain.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Setup & Test Infrastructure</name>
  <files>package.json, tsconfig.json, vitest.config.ts</files>
  <action>
    Initialize a new Node.js TypeScript project.
    Install core dependencies: `@modelcontextprotocol/sdk`, `imapflow`, `nodemailer`, `mailparser`, `cross-keychain`, `zod`, `dotenv`.
    Install dev dependencies: `typescript`, `@types/node`, `@types/nodemailer`, `@types/mailparser`, `vitest`, `ts-node`.
    Configure `tsconfig.json` for ESM and modern Node.js.
    Set up `vitest` as the testing framework.
  </action>
  <verify>
    <automated>npm run test -- --version</automated>
  </verify>
  <done>Project initialized with all dependencies and testing framework configured.</done>
</task>

<task type="auto">
  <name>Task 2: Interface Definitions & Config Management</name>
  <files>src/types/index.ts, src/config.ts</files>
  <action>
    Define TypeScript interfaces for:
    - `EmailAccount`: host, port, user, authType (login/oauth2), useTLS.
    - `Credentials`: password or oauth tokens.
    Implement a simple configuration loader that handles environment variables (using `dotenv`) and Account definitions.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Core types and configuration management implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Keychain Integration & MCP Boilerplate</name>
  <files>src/security/keychain.ts, src/index.ts</files>
  <action>
    Implement `src/security/keychain.ts` using `cross-keychain` to store and retrieve mail account passwords/tokens. Use a unique service name like `com.mcp.mail-server`.
    Implement a basic `src/index.ts` that initializes an MCP `StdioServerTransport` and a `Server` instance.
    Add a placeholder tool `list_accounts` to verify the server is working.
  </action>
  <verify>
    <automated>npx vitest run src/security/keychain.test.ts</automated>
  </verify>
  <done>Keychain integration complete and MCP server boilerplate running.</done>
</task>

</tasks>

<verification>
Check that the MCP server can be started and that credentials can be saved/loaded from Keychain without error.
</verification>

<success_criteria>
- Project builds without errors (`npm run build`).
- Keychain tests pass.
- MCP server starts and responds to `list_tools` via stdio.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity/01-01-SUMMARY.md`
</output>
