---
phase: 01-secure-connectivity
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: [src/security/oauth2.ts, src/protocol/imap.ts, src/protocol/smtp.ts, src/security/keychain.ts]
autonomous: true
requirements: [AUTH-04]
must_haves:
  truths:
    - "Can connect to Gmail/Outlook using OAuth2 tokens"
    - "Tokens can be securely stored and refreshed"
  artifacts:
    - path: "src/security/oauth2.ts"
      provides: "OAuth2 token management and refreshing"
  key_links:
    - from: "src/security/oauth2.ts"
      to: "src/security/keychain.ts"
      via: "token storage"
---

<objective>
Implement OAuth2 authentication support for major providers like Gmail and Outlook, including token storage and refresh logic.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-secure-connectivity/01-01-PLAN.md
@.planning/phases/01-secure-connectivity/01-02-PLAN.md
@.planning/phases/01-secure-connectivity/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OAuth2 Token Management & Storage</name>
  <files>src/security/oauth2.ts, src/security/keychain.ts</files>
  <action>
    Implement `src/security/oauth2.ts` to handle OAuth2 credentials (clientId, clientSecret, accessToken, refreshToken).
    Update `src/security/keychain.ts` to support storing/retrieving JSON-encoded OAuth2 token objects.
    Implement a `refreshAccessToken` utility (e.g., using `fetch` to provider's token endpoint).
  </action>
  <verify>
    <automated>npx vitest run src/security/oauth2.test.ts</automated>
  </verify>
  <done>OAuth2 token management and storage implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update Protocols to use OAuth2</name>
  <files>src/protocol/imap.ts, src/protocol/smtp.ts</files>
  <action>
    Update `ImapClient` and `SmtpClient` to support OAuth2 authentication.
    - `imapflow`: Pass `{ user, accessToken }` in the `auth` object.
    - `nodemailer`: Configure `auth` as `OAuth2` with `user`, `clientId`, `clientSecret`, `refreshToken`, and optionally `accessToken`.
    Ensure the clients can use refreshed tokens from the `OAuth2Service`.
  </action>
  <verify>
    <automated>npx vitest run src/protocol/imap.test.ts src/protocol/smtp.test.ts</automated>
  </verify>
  <done>IMAP and SMTP clients now support OAuth2 connections.</done>
</task>

<task type="auto">
  <name>Task 3: Tool to register OAuth2 tokens</name>
  <files>src/index.ts, src/services/mail.ts</files>
  <action>
    Implement a `register_oauth2_account` tool in `src/index.ts`.
    Note: Initial OAuth2 flow (user consent) should be handled out-of-band for this phase, where the user provides the initial `refreshToken`.
    This tool stores the provided OAuth2 credentials in the Keychain.
  </action>
  <verify>
    <automated>npx vitest run src/services/mail.test.ts</automated>
  </verify>
  <done>Tool for registering OAuth2 credentials implemented.</done>
</task>

</tasks>

<verification>
Configure an account with a valid Google/Outlook refreshToken.
Call `register_oauth2_account`.
Verify that `list_emails` and `send_email` work correctly using OAuth2.
Verify that the `accessToken` can be refreshed if it expires.
</verification>

<success_criteria>
- Securely establishes connections to OAuth2-only mail providers.
- Successfully refreshes expired access tokens.
- OAuth2 credentials are never stored in plaintext.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity/01-04-SUMMARY.md`
</output>
