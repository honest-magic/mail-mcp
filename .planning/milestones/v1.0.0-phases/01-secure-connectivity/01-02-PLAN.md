---
phase: 01-secure-connectivity
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/protocol/imap.ts, src/services/mail.ts, src/utils/markdown.ts, src/index.ts]
autonomous: true
requirements: [IMAP-01, IMAP-02, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "Can list recent emails from INBOX with metadata"
    - "Can read email body content as sanitized Markdown"
  artifacts:
    - path: "src/protocol/imap.ts"
      provides: "IMAP connection and fetch logic"
    - path: "src/utils/markdown.ts"
      provides: "HTML to Markdown conversion"
  key_links:
    - from: "src/services/mail.ts"
      to: "src/protocol/imap.ts"
      via: "calls"
    - from: "src/protocol/imap.ts"
      to: "imapflow"
      via: "protocol library"
---

<objective>
Implement IMAP connectivity using `imapflow` and create MCP tools for listing emails and reading their content as Markdown.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-secure-connectivity/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: IMAP Client & Connection Logic</name>
  <files>src/protocol/imap.ts</files>
  <action>
    Implement an `ImapClient` class using `imapflow`.
    Configure it to handle TLS/SSL connections (AUTH-03).
    Implement a `connect` method that uses credentials retrieved from the Keychain.
    Add a `listMessages` method that fetches recent message metadata (envelope, flags, snippet) from the INBOX.
  </action>
  <verify>
    <automated>npx vitest run src/protocol/imap.test.ts</automated>
  </verify>
  <done>IMAP client implemented with basic listing capability.</done>
</task>

<task type="auto">
  <name>Task 2: Email Reading & Markdown Sanitization</name>
  <files>src/utils/markdown.ts, src/protocol/imap.ts</files>
  <action>
    Implement `src/utils/markdown.ts` using `turndown` (install as new dependency) to convert HTML to Markdown.
    Implement a `fetchMessageBody` method in `ImapClient` that uses `mailparser` to parse the MIME structure.
    Integrate these into a `read_email` tool in `src/index.ts` that returns the body as Markdown.
  </action>
  <verify>
    <automated>npx vitest run src/utils/markdown.test.ts</automated>
  </verify>
  <done>Email content reading and Markdown conversion implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Expose list_emails & read_email Tools</name>
  <files>src/index.ts, src/services/mail.ts</files>
  <action>
    Create `src/services/mail.ts` to orchestrate between the protocol clients and the MCP server.
    Update `src/index.ts` to register `list_emails` and `read_email` tools.
    - `list_emails`: takes `folder` (default "INBOX") and `count`.
    - `read_email`: takes `messageId`.
  </action>
  <verify>
    <automated>npx vitest run src/services/mail.test.ts</automated>
  </verify>
  <done>MCP tools for reading emails are available and functional.</done>
</task>

</tasks>

<verification>
Start the MCP server and use a client (e.g., Inspector) to call `list_emails` and `read_email` with real or mock IMAP credentials.
</verification>

<success_criteria>
- `list_emails` returns a list of recent messages with sender, subject, and date.
- `read_email` returns the email content as a clean Markdown string.
- Connections are securely established using TLS.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity/01-02-SUMMARY.md`
</output>
