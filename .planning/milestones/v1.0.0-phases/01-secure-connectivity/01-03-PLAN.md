---
phase: 01-secure-connectivity
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/protocol/smtp.ts, src/services/mail.ts, src/index.ts]
autonomous: true
requirements: [SMTP-01, SMTP-03, SMTP-04]
must_haves:
  truths:
    - "Can send an email and see it in the Sent folder"
    - "Can create a draft in the Drafts folder"
  artifacts:
    - path: "src/protocol/smtp.ts"
      provides: "SMTP connection and send logic"
  key_links:
    - from: "src/services/mail.ts"
      to: "src/protocol/smtp.ts"
      via: "calls"
    - from: "src/services/mail.ts"
      to: "src/protocol/imap.ts"
      via: "IMAP APPEND for Sent/Drafts"
---

<objective>
Implement SMTP connectivity using `nodemailer` and create MCP tools for sending emails and creating drafts.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-secure-connectivity/01-01-PLAN.md
@.planning/phases/01-secure-connectivity/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMTP Client & Send Logic</name>
  <files>src/protocol/smtp.ts</files>
  <action>
    Implement an `SmtpClient` class using `nodemailer`.
    Configure it to use TLS/SSL (AUTH-03).
    Implement a `send` method that takes `to`, `subject`, and `body` (HTML/text).
    Retrieve credentials from the Keychain.
  </action>
  <verify>
    <automated>npx vitest run src/protocol/smtp.test.ts</automated>
  </verify>
  <done>SMTP client implemented with email sending capability.</done>
</task>

<task type="auto">
  <name>Task 2: Send Email tool with Sync to Sent</name>
  <files>src/index.ts, src/services/mail.ts, src/protocol/imap.ts</files>
  <action>
    Implement an `appendMessage` method in `ImapClient` (in `src/protocol/imap.ts`) to handle IMAP `APPEND` command.
    In `src/services/mail.ts`, implement `sendAndArchive` logic that sends an email via SMTP and then appends it to the IMAP "Sent" folder.
    Expose a `send_email` tool in `src/index.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/services/mail.test.ts</automated>
  </verify>
  <done>Send Email tool with automatic syncing to Sent folder implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Create Draft Tool</name>
  <files>src/index.ts, src/services/mail.ts</files>
  <action>
    Implement a `create_draft` tool that uses the IMAP `APPEND` command to save a message to the "Drafts" folder.
    This tool should accept `to`, `subject`, and `body`.
  </action>
  <verify>
    <automated>npx vitest run src/services/mail.test.ts</automated>
  </verify>
  <done>Create Draft tool implemented.</done>
</task>

</tasks>

<verification>
Use an MCP client to call `send_email` and `create_draft`.
Verify that the sent email arrives at the destination AND appears in the sender's "Sent" folder via IMAP.
Verify that the created draft appears in the "Drafts" folder.
</verification>

<success_criteria>
- Emails are successfully sent via SMTP.
- Sent emails are correctly archived in the IMAP "Sent" folder.
- Drafts are correctly created in the IMAP "Drafts" folder.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-connectivity/01-03-SUMMARY.md`
</output>
